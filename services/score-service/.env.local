# Local CSV mode — no Postgres required.
# Copy to .env or export these variables before running.
DATA_MODE=csv
CSV_DATA_PATH=data/mock/student_score_dataset.csv
MAX_SESSIONS=20
DEBUG=false
ALLOWED_ORIGINS=["http://localhost:3000","http://localhost:8002"]
